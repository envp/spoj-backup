#include <stdio.h>

int main() {
	int t, ng, nm;
	int *godzilla, *mecha
	scanf("%d", &t);
	while(t--) {
		scanf("%d %d", &ng, &nm);
		godzilla = (int *)malloc(ng*sizeof(int));
		mecha = (int *)malloc(nm*sizeof(int));
		
	}
	return 0;
}