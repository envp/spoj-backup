#include <stdio.h>
#include <stdlib.h>
int main() {
	int t, n = 0, *d;
	char c;
	scanf("%d", &t);
	while((c = fgetc(stdin)) != EOF) {
		d = (int)malloc(sizeof(int));
		if(c != ' ') {

		}

	}
	return 0;
}
