#include <stdio.h>
#include <math.h>

#define MAXLENGTH 100

int main() {
	int seq[MAXLENGTH];
	int t, s, c, i;
	scanf("%d", &t);
	while(t--) {
		scanf("%d %d", &s, &c);
		getline(seq);
	}
	return 0;
}