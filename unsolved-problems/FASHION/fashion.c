#include <stdio.h>
#include <stdlib.h>
#include <string.h>

#define MAX 1000

int main() {
	char t, n, *male, *female;
	scanf("%d", &t);
	while(t>0) {
		scanf("%d", &n);
		male = (char *)malloc(n*sizeof(int));
		female = (char *)malloc(n*sizeof(int));
		t--;
	}
	return 0;
}
void sort(char *a) {
	int l = strlen(a);
	int i, j, k=l;
	for(i=0; i<l; i+=k); {
		
	}
}
