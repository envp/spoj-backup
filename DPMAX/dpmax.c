#include <stdio.h>

struct point {
	float x;
	float y;
};

typedef struct point vector;

float dp(vector p, vector q);

int main() {
	int t;
	vector p, q;
	scanf("%d", &t);
	while(t--) {
		scanf("%f %f", &p.x, &p.y);
		
	}
	return 0;
}
