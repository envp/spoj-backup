package main

import {
    "fmt"
    "math"
}

type Vertex struct {
    x double
    y double
}

func main() {
        Vertex u, v;
        fmt.Scanf("(%f, %f)", &u.x, &u.y)
        while(fmt.Scanf("(%f, %f)", &v.x, &v.y) == 2) {
            
        }
}
