package main

import "fmt"
import "Math"

type Vertex struct {
    x double
    y double
};

func main() {
		u, v Vertex 
        fmt.Scanf("(%f, %f)", &u.x, &u.y)
        while(fmt.Scanf("(%f, %f)", &v.x, &v.y) == 2) {
            
        }
}
