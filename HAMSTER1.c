#include <stdio.h>
#include <math.h>

#define G 10
#define PI 3.14159265359
int main() {
	int t;                          /*	Testcases	*/
	int v0, k1, k2;                 /*	Initial speed and parameters for judging	*/
	double x, y, phi, ppints;
	while(t--) {
		scanf("%d %d %d", v0, k1, k2);
		if(k1 == 0) {
			phi = ;
			points = ;
		}
		else if (k2 == 0) {
			phi = ;
		}
	}
	return 0;
}
